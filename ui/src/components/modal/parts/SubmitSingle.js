import React from 'react'
import Button from 'material-ui/Button'

const SubmitSingle = ({name}) => (
  <div className="form-buttons">
    <Button variant="raised" type="submit">name</Button>
  </div>
)

export default SubmitSingle
